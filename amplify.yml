version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - rm -f .env.production
        - touch .env.production
        - |
          for var in OPENAI_API_KEY OPENAI_MODEL OPENAI_TIMEOUT_MS OPENAI_MAX_TOKENS OPENAI_TEXT_MAX_TOKENS; do
            value="$(printenv "$var" || true)"
            if [ -n "$value" ]; then
              printf "%s=%s\n" "$var" "$value" >> .env.production
            fi
          done
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
